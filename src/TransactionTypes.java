//Constant values are based on the names of the classes
//so that there is no need to add a transaction type attribute in Transactions Class
public enum TransactionTypes {
    MoneyTransfer,
    BillsPayment,
    BuyLoad,
    AddAccountCredit,
    AddGameCredit
}
